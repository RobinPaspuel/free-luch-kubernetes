apiVersion: v1
kind: Service
metadata:
  name: {{ include "free-lunch.fullname" . }}
  labels:
    {{- include "free-lunch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "free-lunch.selectorLabels" . | nindent 4 }}
